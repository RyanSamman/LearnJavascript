[Javascript Shorthands](https://www.sitepoint.com/shorthand-javascript-techniques/)

Careful when using `let x = variable || "value"`, if `variable` is `false` or `0` then `x` will equate to `"value"`
